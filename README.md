# Virtual-Background

A real-time virtual background application that removes your webcam background and replaces it with images. Press 'a' to switch to previous background, 'd' for next background, and 'q' to quit.

Built with cvzone's SelfiSegmentation and OpenCV for video capture and background removal.